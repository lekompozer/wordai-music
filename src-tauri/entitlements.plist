<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Allow loading dylibs that don't have a matching Team ID (e.g. Homebrew openssl) -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- Allow JIT compilation (needed by llama.cpp inference) -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <!-- Allow unsigned executable memory (llama.cpp uses it for fast inference) -->
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <!-- Outgoing network connections (API calls, HuggingFace download) -->
  <key>com.apple.security.network.client</key>
  <true/>
</dict>
</plist>
